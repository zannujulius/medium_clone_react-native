import React, { createContext, useMemo } from "react";

export const AuthContext = createContext();

// const AuthContextProvoder = ({ children }) => {
//   return <AuthContext.Provider>{children}</AuthContext.Provider>;
// };
